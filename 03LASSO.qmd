---
title: "LASSO"
format:
  typst:
    fig-format: retina
    mainfont: "Hiragino Mincho ProN"
    number-sections: true
  revealjs:
    incremental: true 
    slide-number: true
    html-math-method: katex
    embed-resources: true
execute:
  echo: false
  eval: true
  warning: false
  message: false
  dev: 'ragg_png'
---

# LASSO

```{r}
library(tidyverse)
set.seed(1)

data = read_csv("Public/example.csv")
```


## 目標

- データ主導の線型モデルの推定方法を学ぶ

  - 代表的な手法であるLASSOを紹介
  
- "モデルの複雑性を調整する"というイメージを把握する

## 復習

- 予測モデルの構築: データをモデルに変換する

  - 変換 $=$ 推定/学習
  
- 変換手順は、人間が決める必要がある

  - 変換手順 $=$ 定式化と推定方法/アルゴリズム

```{mermaid}
flowchart LR
  D[データ] --> |投入| A[推定方法] --> |推定| M[予測モデル]
```

## 定式化の例: ベンチマーク

-  $$Price\simeq\beta_0 + \beta_1\times \underbrace{RoomNumber}_{部屋数}$$

## 定式化の例: より単純なモデル

- $$Price\simeq\beta_0$$

  - $\beta_1=0$ と"人間"が決めている $\rightarrow$ データに決めさせる部分が減っている
  
    - $\beta_0= Price$ の単純平均 (東京全体での平均価格)が予測"モデル"となる 

## 定式化の例: より複雑なモデル

- $$Price\simeq\beta_0 + \beta_1\times RoomNumber + \beta_2\times RoomNumber^2$$ $$+\beta_3\times RoomNumber^3$$

- $\beta_2=\beta_3=0$ と"人間"が決れば、ベースモデルと一致

  - より多くをデータに決定させる定式化

## 定式化の例: 凄く複雑なモデル

- $$Price\simeq\beta_0 + \beta_1\times RoomNumber + \beta_2\times RoomNumber^2$$ $$+\beta_3\times RoomNumber^3 + \beta_4\times RoomNumber^4$$

- $\beta_4=0$ と"人間"が決れば、より複雑なモデルと一致

## 例: ベンチマーク

```{r,dev='ragg_png'}
Test <- tibble(
  RoomNumber = seq(1,5,0.01)
)

E_Y_X <- lm(Price ~ 0 + factor(RoomNumber), data) |> 
  coef()

Test$E_Y <- data$Price |> mean()

Test$Linear <- lm(Price ~ RoomNumber, data) |> 
  predict(Test)

Test$Quad <- lm(Price ~ poly(RoomNumber,3), data) |> 
  predict(Test)

Test$Complex <- lm(Price ~ poly(RoomNumber,4), data) |> 
  predict(Test)

Test |> 
  mutate(
    E_Y_X = case_when(
      RoomNumber == 1 ~ E_Y_X[1],
      RoomNumber == 2 ~ E_Y_X[2],
      RoomNumber == 3 ~ E_Y_X[3],
      RoomNumber == 4 ~ E_Y_X[4],
      RoomNumber == 5 ~ E_Y_X[5],
      .default = NA
    )
  ) |> 
  ggplot(
    aes(
      x = RoomNumber
    )
  ) +
  geom_point(
    aes(
      y = E_Y_X
    )
  ) +
  geom_line(
    aes(
      y = Linear
    )
  ) +
  theme_bw() +
  ylab("平均価格")
```

- ５部屋物件の予測価格 $\simeq 100$

## 例: 単純なモデル

```{r,dev='ragg_png'}
Test |> 
  mutate(
    E_Y_X = case_when(
      RoomNumber == 1 ~ E_Y_X[1],
      RoomNumber == 2 ~ E_Y_X[2],
      RoomNumber == 3 ~ E_Y_X[3],
      RoomNumber == 4 ~ E_Y_X[4],
      RoomNumber == 5 ~ E_Y_X[5],
      .default = NA
    )
  ) |> 
  ggplot(
    aes(
      x = RoomNumber
    )
  ) +
  geom_point(
    aes(
      y = E_Y_X
    )
  ) +
  geom_line(
    aes(
      y = Linear
    ),
    linetype = "dotted"
  ) +
  geom_line(
    aes(
      y = E_Y
    )
  ) +
  theme_bw() +
  ylab("平均価格")
```

- ５部屋物件の予測価格 $\simeq 45$

## 例: 複雑なモデル

```{r,dev='ragg_png'}
Test |> 
  mutate(
    E_Y_X = case_when(
      RoomNumber == 1 ~ E_Y_X[1],
      RoomNumber == 2 ~ E_Y_X[2],
      RoomNumber == 3 ~ E_Y_X[3],
      RoomNumber == 4 ~ E_Y_X[4],
      RoomNumber == 5 ~ E_Y_X[5],
      .default = NA
    )
  ) |> 
  ggplot(
    aes(
      x = RoomNumber
    )
  ) +
  geom_point(
    aes(
      y = E_Y_X
    )
  ) +
  geom_line(
    aes(
      y = Linear
    ),
    linetype = "dotted"
  ) +
  geom_line(
    aes(
      y = E_Y
    ),
    linetype = "dotted"
  ) +
  geom_line(
    aes(
      y = Quad
    )
  ) +
  theme_bw() +
  ylab("平均価格")
```

- ５部屋物件の予測価格 $\simeq 191$

## 例: 凄く複雑なモデル

```{r,dev='ragg_png'}
Test |> 
  mutate(
    E_Y_X = case_when(
      RoomNumber == 1 ~ E_Y_X[1],
      RoomNumber == 2 ~ E_Y_X[2],
      RoomNumber == 3 ~ E_Y_X[3],
      RoomNumber == 4 ~ E_Y_X[4],
      RoomNumber == 5 ~ E_Y_X[5],
      .default = NA
    )
  ) |> 
  ggplot(
    aes(
      x = RoomNumber
    )
  ) +
  geom_point(
    aes(
      y = E_Y_X
    )
  ) +
  geom_line(
    aes(
      y = Linear
    ),
    linetype = "dotted"
  ) +
  geom_line(
    aes(
      y = E_Y
    ),
    linetype = "dotted"
  ) +
  geom_line(
    aes(
      y = Quad
    ),
    linetype = "dotted"
  ) +
  geom_line(
    aes(
      y = Complex
    )
  ) +
  theme_bw() +
  ylab("平均価格")

```

- ５部屋物件の予測価格 $\simeq 507$

## 過剰適合/過学習

- 複雑なモデルは、より多くをデータが決定し、データ上の平均値とモデルが完全一致している

  - データに最も適合するが、、、
  
    - 予測性能が低いケースが多い

- **過学習/過剰適合:** モデルが、データと過度に適合する(事例から過度に学び過ぎてしまう)現象

    - 非常に少数の事例から生じたデータ特徴も、モデルに反映してしまう
    
      - 5部屋の物件は3事例しか存在しない
    
# 複雑性への罰則

## 目標

- データにより多くのことを決めさせるには、複雑なモデルを推定する必要がある

  - 複雑なモデルをOLS推定すると、データへの過剰適合が生じる

- 複雑なモデルを「データに程よく適合させたい」

## 例え話

- 入門経済学: 生産方法を企業の自主的な意思決定に任せると

  - 同じ生産量を達成する方法の中で、最も費用が少ない方法が選ばれる
  
    - 希少な資源の利用を減らせ、それなりに望ましい
    
  - 一般に"負の外部性"が生じる
  
    - 温室効果ガスの過剰排出等
    
  - 社会的に望ましい水準に誘導するための政策が必要
  
    - 総量規制、環境税、補助金等
    
## 罰則付き回帰

- 線型モデル $Y\simeq\beta_0 +\beta_1X_1+..$ を推定

- 以下を最小にするように$\beta$を推定 $$\underbrace{データへの不適合度}_{(Y-予測値)^2のデータ上の平均} + 複雑性への罰則$$

## OLSの再解釈

- OLS: 以下を最小にするように推定 $$データへの不適合度$$

- 定式化を前提に、データへの適合**のみ**を目指す推定方法

  - 過剰適合の原因

## 罰則の定式化

- $$複雑性への罰則=\underbrace{\lambda}_{税率}\times 複雑性の測定値$$
  
  - LASSOにおいては、$$複雑性の測定値=|\beta_1|+|\beta_2|..$$

- $\lambda$ は予測性能が良くなりそうな値を設定する

  - いろいろな方法が提案されている

  - 本講義では、hdm packageで実装されている理論的指標を用いる

## $\lambda$ の影響

- 最も単純なモデル (複雑性の測定値 $=$ 0)は、 $$\beta_1=\beta_2=..=0$$

  - $Y\simeq \beta_0=単純平均$
  
- $|\beta_1|$ が大きくなれば、予測値が $X_1$ により反応する
  
  - より複雑な予測モデル

## $\lambda$ の影響

- $\lambda\rightarrow\infty$ であれば、複雑性の低下が最優先

  - $\beta_1=\beta_2=..=0$

  - 予測モデル $=$ 単純平均

## $\lambda$ の影響

- $\lambda=0$ であれば、データへの当てはまりのみを追求

  - OLS!!!

  - 予測モデル $=$ OLSで推定されたモデル
  
## 例

```{r,dev='ragg_png'}
#| echo: true

hdm::rlasso(Price ~ Size + Tenure + Distance + District, data, post = FALSE)
```

## 例: OLS

```{r,dev='ragg_png'}
#| echo: true

lm(Price ~ Size + Tenure + Distance + District, data)
```

## 例

```{r,dev='ragg_png'}
set.seed(111)
Test <- tibble(
  RoomNumber = seq(1,5,0.01)
)

E_Y_X <- lm(Price ~ 0 + factor(RoomNumber), data) |> 
  coef()

Test$ols <- lm(Price ~ poly(RoomNumber,4), data) |> 
  predict(Test)

cv <- glmnet::cv.glmnet(
  y = data$Price,
  x = data |> model.matrix(~ 0 + poly(RoomNumber,4), data =_)
)

Test$lasso <- glmnet::glmnet(
  y = data$Price,
  x = data |> model.matrix(~ 0 + poly(RoomNumber,4), data =_),
 lambda = cv$lambda.min
) |> 
  predict(Test |> model.matrix(~ 0 + poly(RoomNumber,4), data =_))


Test |> 
  mutate(
    E_Y_X = case_when(
      RoomNumber == 1 ~ E_Y_X[1],
      RoomNumber == 2 ~ E_Y_X[2],
      RoomNumber == 3 ~ E_Y_X[3],
      RoomNumber == 4 ~ E_Y_X[4],
      RoomNumber == 5 ~ E_Y_X[5],
      .default = NA
    )
  ) |> 
  ggplot(
    aes(
      x = RoomNumber
    )
  ) +
  geom_point(
    aes(
      y = E_Y_X
    )
  ) +
  geom_line(
    aes(
      y = ols,
      color = "OLS(税率 = 0)"
    )
  ) +
  geom_line(
    aes(
      y = lasso,
      color = "LASSO(税率 = 1.95)"
    )
  ) +
  theme_bw() +
  ylab("平均価格")
```

## 例

```{r,dev='ragg_png'}

Test$lasso_high <- glmnet::glmnet(
  y = data$Price,
  x = data |> model.matrix(~ 0 + poly(RoomNumber,4), data =_),
 lambda = 10
) |> 
  predict(Test |> model.matrix(~ 0 + poly(RoomNumber,4), data =_))

Test$lasso_middle <- glmnet::glmnet(
  y = data$Price,
  x = data |> model.matrix(~ 0 + poly(RoomNumber,4), data =_),
 lambda = 0.01
) |> 
  predict(Test |> model.matrix(~ 0 + poly(RoomNumber,4), data =_))

Test |> 
  mutate(
    E_Y_X = case_when(
      RoomNumber == 1 ~ E_Y_X[1],
      RoomNumber == 2 ~ E_Y_X[2],
      RoomNumber == 3 ~ E_Y_X[3],
      RoomNumber == 4 ~ E_Y_X[4],
      RoomNumber == 5 ~ E_Y_X[5],
      .default = NA
    )
  ) |> 
  ggplot(
    aes(
      x = RoomNumber
    )
  ) +
  geom_point(
    aes(
      y = E_Y_X
    )
  ) +
  geom_line(
    aes(
      y = lasso,
      color = "LASSO(税率 = 1.95)"
    )
  ) +
  geom_line(
    aes(
      y = lasso_high,
      color = "LASSO(税率 = 10)"
    )
  ) +
  geom_line(
    aes(
      y = lasso_middle,
      color = "LASSO(税率 = 0.01)"
    )
  ) +
  theme_bw() +
  ylab("平均価格")
```

## LASSOの性質: 変数選択

- LASSOの重要な性質は変数選択

  - モデルから変数を除外する
  
- 例: モデル $$Price\simeq\beta_0+\beta_1Size+\beta_2Tenure$$ から、Tenureを除外する

  - $$Price\simeq\beta_0+\beta_1Size+\underbrace{\beta_2}_{=0}Tenure$$


## LASSOの性質

- 罰則の定式化より、$\beta$ の推定値が厳密に0となりうる

  - 該当する変数が除外される
  
- OLSなど他の推定方法では、一般に厳密に0とはならない

  - 変数が除外されない
  
    - 除外したいのであれば、人間が推定前に除外しないといけない
    
- LASSOは、"データ主導の変数選択"を行う

## Takeaway

- 一般に、OLSは複雑なモデルの推定が苦手

  - 過剰適合を避けるために、多くの事例数を要求
  
    - 経験則として、$\beta$ の数が事例数の1/3を超えると予測性能が悪化しやすい
    
- 機械学習 $=$ 複雑なモデルを、過剰適合を避けながら推定する方法を提案

  - LASSOは代表格